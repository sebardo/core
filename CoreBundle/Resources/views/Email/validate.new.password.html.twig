
{% block subject %}{{ 'recovery.success.subject' | trans }}{% endblock %}

{% block body_text %}
{% autoescape false %}
{{ 'recovery.success.email.title' | trans({ '%name%': name }) }}

{{ 'recovery.success.email.message' | trans }}
{% endautoescape %}
{% endblock %}


{% block body_html %}
{% spaceless %}
    {% if no_headers is not defined %}
        {{ include('CoreBundle:Email:_header.html.twig') }}
    {% else %}
    {% endif %}

    <p style="padding:0 0 20px 0; margin:0;">{{ 'recovery.success.email.title' | trans({ '%name%': name }) }}</p>
    
    <p style="padding:0 0 20px 0; margin:0;">{{ 'recovery.success.email.message' | trans }}</p>

    {% if no_headers is not defined %}
        {{ include('CoreBundle:Email:_footer.html.twig') }}
    {% else %}
    {% endif %}
    
{% endspaceless %}
{% endblock %}

