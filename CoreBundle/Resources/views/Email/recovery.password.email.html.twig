
{% block subject %}{{ 'account.password.recovery.title' | trans }}{% endblock %}

{% block body_text %}
{% autoescape false %}
{{ 'account.password.recovery.email.title' | trans({ '%name%': name }) }}

{{ 'account.password.recovery.email.message' | trans }}

<a href="{{core.server_base_url}}{{ path('recovery_password_form' , {'hash': hash, 'email': email}) }}">Recuperar</a><br><br>

{{ 'validate.email.valid24hs' | trans }}
{% endautoescape %}
{% endblock %}


{% block body_html %}
{% spaceless %}
    {{ include('CoreBundle:Email:_header.html.twig') }}

    <p style="padding:0 0 20px 0; margin:0;">{{ 'account.password.recovery.email.title' | trans({ '%name%': name }) }}</p>

    <p style="padding:0 0 20px 0; margin:0;">{{ 'account.password.recovery.email.message' | trans }}</p>
    
    <p style="padding:0 0 20px 0; margin:0;"><a href="{{core.server_base_url}}{{ path('recovery_password_form' , {'hash': hash, 'email': email}) }}">Recuperar</a></p>

    <p style="padding:0; margin:0;">{{ 'validate.email.valid24hs' | trans }}</p>

    {{ include('CoreBundle:Email:_footer.html.twig') }}
{% endspaceless %}
{% endblock %}
