{% extends core['extended_layout'] ? core['extended_layout'] : 'CoreBundle:Base:base.layout.html.twig' %}

{% block  content %}
   

    <div class="col-sm-4"></div>        
    <div class="col-sm-4" style="text-align: center">
        <div class="container-login">
           <h2>Soy cliente</h2>
           <hr>
           {% if error is defined and error %}
               <div>{{ error.message }}</div>
           {% endif %}
           <form id="login-form" action="{{ path('login_check') }}" method="post">
               <div class="form-group">
                   <label class="control-label" for="_username">Email</label>
                   <div>
                       <input class="form-control" type="email" name="_username" id="_username" value="" required="required">
                   </div>
               </div>    
               <div class="form-group">
                   <label class="control-label" for="_password">Contraseña</label>
                   <div>
                        <input class="form-control" type="password" name="_password" id="_password" required="required">
                   </div>
               </div>         

               <div class="clearfix prettycheckbox labelright  blue ">
                   <div class="checkbox">
                       <label>
                         <input type="checkbox" value=""> No cerrar sesión </label>
                         <a style="float: right" href="/resetting/request" title="No recuerdo mi contraseña">No recuerdo mi contraseña</a>
                   </div>
               </div>

               <button type="submit" class="btn btn-primary btn-block">
                   Entrar
               </button>

           </form>
       </div>
   </div>
    <div class="col-sm-4"></div>               
{% endblock %}
